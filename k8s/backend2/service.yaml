apiVersion: v1
kind: Service
metadata:
  name: backend2-service
  namespace: verified-backend
  labels:
    app: backend2
spec:
  selector:
    app: backend2
  ports:
    - name: http
      port: 3000
      targetPort: 3000
  type: ClusterIP
